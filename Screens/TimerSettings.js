import { View, Text } from "react-native";
import React from "react";

const TimerSettings = () => {
  return (
    <View>
      <Text>TimerSettings</Text>
    </View>
  );
};

export default TimerSettings;
